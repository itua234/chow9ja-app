 {/* {isErrorVisible && (
    <View style={{
        ...StyleSheet.absoluteFillObject,
        backgroundColor: 'rgba(0, 0, 0, 0.5)',
        justifyContent: 'center',
        alignItems: 'center',
        zIndex: 10,
    }}>
        <View style={{
            backgroundColor: 'white',
            padding: 20,
            borderRadius: 10,
            width: '90%',
            alignItems: 'center',
        }}>
            <Text style={{ color: 'red', fontSize: 18, marginBottom: 10 }}>
                {msg}
            </Text>
            <TouchableOpacity onPress={() => setErrorVisible(false)}>
                <Text style={{ color: 'blue', fontSize: 16 }}>Dismiss</Text>
            </TouchableOpacity>
        </View>
    </View>
)} */}